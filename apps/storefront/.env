# =============================================================================
# Medusa Storefront Environment Variables
# =============================================================================
# Copy this file to .env and update the values for your environment.
#
# IMPORTANT: Variables prefixed with PUBLIC_ are exposed to the browser.
# Never put sensitive data in PUBLIC_ variables.
# =============================================================================

# -----------------------------------------------------------------------------
# Medusa Backend URL
# -----------------------------------------------------------------------------
# The URL where your Medusa backend is running.
# For local development, this is typically http://localhost:9000
# For production, use your deployed Medusa backend URL.

# Server-side only (used during build/SSR)
MEDUSA_BACKEND_URL=https://api.majalahpdf.my.id

# Client-side (exposed to browser for cart/checkout interactions)
PUBLIC_MEDUSA_BACKEND_URL=https://api.majalahpdf.my.id

# -----------------------------------------------------------------------------
# Medusa Publishable API Key
# -----------------------------------------------------------------------------
# The publishable API key from your Medusa admin dashboard.
# This key is safe to expose publicly - it only grants access to store endpoints.
#
# To get this key:
# 1. Go to Medusa Admin -> Settings -> API Keys
# 2. Create or copy an existing publishable key
# 3. The key format is: pk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Server-side only (used during build/SSR)
MEDUSA_PUBLISHABLE_KEY=pk_5be47d74930b81d077f3e51aa9c185d8a669dcc7fee5cd0fa9428d842c00959b

# Client-side (exposed to browser for cart/checkout interactions)
PUBLIC_MEDUSA_PUBLISHABLE_KEY=pk_4cdce5e0bb12aa7dd2bf19f36bc48b90a671e98d65b7a329f73ded68ffd50ae2

# -----------------------------------------------------------------------------
# Site URL (for sitemap and canonical URLs)
# -----------------------------------------------------------------------------
# The public URL where your storefront is deployed.
# Used for generating sitemap.xml and canonical meta tags.
# For local development, this can be left as example.com (sitemap generation will still work).
# For production, set this to your actual domain.

SITE_URL=https://shop.majalahpdf.my.id
