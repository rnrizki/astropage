{
  "name": "medusa-astro-starter",
  "author": "Christian Anese <christian.anese@gmail.com>",
  "private": false,
  "packageManager": "pnpm@10.28.2",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "start": "turbo start",
    "lint": "turbo lint",
    "test": "turbo test",
    "backend:seed": "turbo seed --filter=@canese/backend",
    "backend:dev": "turbo dev --filter=@canese/backend",
    "storefront:dev": "turbo dev --filter=@canese/storefront"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "turbo": "^2.8.1"
  }
}
